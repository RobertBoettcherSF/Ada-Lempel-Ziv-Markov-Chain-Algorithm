-- main.adb
-- A minimal main entry point to satisfy compilation of the standard binary.

with Ada.Text_IO; use Ada.Text_IO;

procedure Main is
begin
   Put_Line ("LZMA library compiled successfully. Please run 'make test' to execute logic.");
end Main;
